apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bot-data
spec:
  # SQLite не переживёт параллельную запись с нескольких узлов, да и под всегда
  # один — ReadWriteOnce достаточно.
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
